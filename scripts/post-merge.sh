#!/bin/bash
# Post-merge setup for Dariem Garcia Portfolio
# Static site — no dependencies to install, no migrations to run.
set -e

echo "Post-merge setup complete (static site, nothing to install)."
